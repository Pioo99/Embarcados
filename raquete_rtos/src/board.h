#pragma once
#include "stm32f4xx.h"

/* ── Dimensões do display ───────────────────────────────────────── */
#define LCD_W 240
#define LCD_H 240

/* ── Pinos do ST7789 ────────────────────────────────────────────── */
#define LCD_DC_PORT   GPIOB
#define LCD_DC_PIN    0
#define LCD_RST_PORT  GPIOB
#define LCD_RST_PIN   1
#define LCD_BLK_PORT  GPIOB
#define LCD_BLK_PIN   6
#define LCD_CS_PORT   GPIOB
#define LCD_CS_PIN    10

/* SPI1: SCK=PA5, MOSI=PA7 */

/* ── Piezos ─────────────────────────────────────────────────────
   6 piezos lidos por ADC1 + DMA (scan contínuo, 1 canal por piezo).
   Pinos escolhidos para não colidir com SPI1 (PA5/PA7) nem LCD (PB0/PB1):
     P0→PA0(IN0) P1→PA1(IN1) P2→PA2(IN2)
     P3→PA3(IN3) P4→PA4(IN4) P5→PA6(IN6)
   ────────────────────────────────────────────────────────────── */
#define N_PIEZOS   6

/* ── IMU (BNO055 em I2C1) ───────────────────────────────────────
   SCL→PB8  SDA→PB9   endereço 0x28
   ────────────────────────────────────────────────────────────── */

/* ── Regiões do heatmap ─────────────────────────────────────────
   0=centro, 1-4=bordas, 5-8=cantos
   ────────────────────────────────────────────────────────────── */
#define N_REGIOES  9

/* ── Cores RGB565 ───────────────────────────────────────────────── */
#define C_BLACK  0x0000
#define C_WHITE  0xFFFF
#define C_RED    0xF800
#define C_GREEN  0x07E0
#define C_BLUE   0x001F
#define C_YELL   0xFFE0
#define C_CYAN   0x07FF
#define C_MAG    0xF81F
#define C_GRAY   0x4208
#define C_ORANGE 0xFD20

/* ── Delay — implementado em main.c (DWT), usado por st7789.c,
      imu.c e tarefas ─────────────────────────────────────────────── */
void delay_ms(uint32_t ms);
void delay_us(uint32_t us);
